// Reuse the Open Graph image for the Twitter/X card so twitter:image is
// populated explicitly (not just relied on via og:image fallback).
export { default, alt, size, contentType } from "./opengraph-image"
